kind: TrafficTarget
apiVersion: access.smi-spec.io/v1alpha3
metadata:
  name: microservice-b
spec:
  destination:
    kind: ServiceAccount
    name: microservice-b-sa
    namespace: api
  rules:
  - kind: HTTPRouteGroup
    name: microservices-b-routes
    matches:
    - url-caller
  sources:
  - kind: ServiceAccount
    name: microservice-web-sa
    namespace: api
---
apiVersion: specs.smi-spec.io/v1alpha4
kind: HTTPRouteGroup
metadata:
  name: microservices-b-routes
spec:
  matches:
  - name: url-caller
    pathRegex: /URLCaller/depth/.+
    methods:
    - GET